spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/onlineexamdb
    username: biboran
    password: biboran
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true
